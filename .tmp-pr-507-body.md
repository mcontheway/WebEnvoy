## 摘要

- 变更目的：冻结 `xhs.detail` current v1 canonical identity 基线，只回答 `note_id` only、`image_scenes` 当前不进入 identity，并补齐 current-detail artifact 的最小 `source_note_id -> note_id` 解析规则；同时不再让 `#505` 反向承载 `#504` 的 request-context 行为所有权。
- 主要改动：
  - 新增 `FR-0026-xhs-detail-canonical-identity` 正式套件，覆盖 `spec.md`、`plan.md`、`TODO.md`、`contracts/detail-canonical-identity.md`、`data-model.md`、`research.md`、`risks.md`
  - 冻结 current v1 `xhs.detail` canonical identity 为 `note_id` only
  - 收窄 formal 结论：当前只冻结 current-detail artifact 的最小 `source_note_id -> note_id` 解析规则，不把它扩写为更广 transport alias、route admission 或其他 request/artifact mapping 规则
  - 明确 `source_note_id` 不进入 frozen identity baseline，也不成为第二个 identity 字段
  - 冻结 `image_scenes` 当前不进入 canonical identity，也不得成为额外 identity discriminator
  - 删除 `compatibility`、`rejected-source matching`、`template reuse`、`shape`、`shape_key`、`eligibility` 等 request-context 语义的 de facto formal 冻结，明确这部分不属于已 merge 的 `#504` formal freeze，而由后续实现链路承接
  - 明确 future identity expansion 或更广 `source_note_id` alias/derivation freeze 只能基于新的仓库证据和新的 spec 修订进入

## 关联事项

- Issue: #505
- Closing: Refs #505
- Context: #445, #500, #502, #504

## 风险级别

- `high`
- 判断依据：变更落在 `docs/dev/specs/**` 正式契约区，并冻结 detail canonical identity 边界，属于高风险 formal spec review 范围。

## 验证

- 已执行：
  - `bash scripts/docs-guard.sh`
  - `bash scripts/spec-guard.sh`
  - `bash scripts/check-pr-purity.sh docs/FR-0026-xhs-detail-canonical-identity main`
  - `git diff --check`
- 未执行：
  - guardian rerun：将在本次 head 更新后重新执行
  - runtime / extension / live rerun：本 PR 未改实现代码，不适用

## integration_check

- integration_applicable（`yes` / `no`）: no
- integration_touchpoint（`none` / `check_required` / `active` / `blocked` / `resolved`）: none
- integration_ref: none
- shared_contract_changed（`yes` / `no`）: no
- external_dependency（`none` / `syvert` / `webenvoy` / `both`）: none
- merge_gate（`local_only` / `integration_check_required`）: local_only
- contract_surface（`none` / `execution_provider` / `ids_trace` / `errors` / `raw_normalized` / `diagnostics_observability` / `runtime_modes` / `integration_governance`）: none
- joint_acceptance_needed（`yes` / `no`）: no
- integration_status_checked_before_pr（`yes` / `no`）: no
- integration_status_checked_before_merge（`yes` / `no`）: no

补充说明：

- 当前 PR 只冻结本仓库内 detail canonical identity，不承载跨仓共享契约或联合验收。

## gate_applicability（对 formal spec review PR、live evidence 治理落库/治理维护 PR，以及所有落入真实 live evidence 专项门禁的 PR 必填）

- review_lane（`general_pr` / `formal_spec_review_pr` / `governance_landing_pr` / `governance_maintenance_pr`）: formal_spec_review_pr
- governance_context_issue_ref（仅治理相关 lane 使用；`#310` 仅用于 `governance_landing_pr`；其他治理维护使用独立 issue；非治理场景写 `null`）: null
- governance_scope_targets（仅治理相关 lane 时填写冻结目标文件数组；必须精确列出 `AGENTS.md`、`docs/dev/AGENTS.md`、`code_review.md`、`docs/dev/review/guardian-review-addendum.md`、`.github/PULL_REQUEST_TEMPLATE.md`；其他场景写 `[]`）: []
- in_scope（`true` / `false`）: false
- trigger_reasons（命中触发原因时填写数组；不适用写 `[]`）: []
- n_a_allowed（`true` / `false`）: true

补充说明：

- 当前 PR 是 formal spec review PR，不属于治理 lane，也不落入真实 live evidence 专项门禁。

## live_evidence_record（仅当 `gate_applicability.in_scope=true` 时必填；若 `in_scope=false && n_a_allowed=true`，整块可写 `N/A` 或 `null`）

N/A

## 作者执行现场自述（供 review 参考）

- 本次执行现场：`#505` spec-only lane；只写 `FR-0026` 套件，未触碰 runtime / extension / tests 实现
- worktree / clone 路径：`/Users/mc/dev/WebEnvoy-worktrees/FR-0026-505`
- 是否保持单 worktree 单 issue/PR：是
- PR 创建后是否扩 scope（如有，拆分到哪一个 PR）：否；command surface 与 request-context baseline 已留在 `#504 / FR-0025`，未冻结的 request-shape / reuse 语义留给后续实现链路
- 纯度预检门禁执行记录（命令与结果）：
  - `bash scripts/check-pr-purity.sh docs/FR-0026-xhs-detail-canonical-identity main` -> 通过
  - `bash scripts/spec-guard.sh` -> 通过
  - `bash scripts/docs-guard.sh` -> 通过
  - `git diff --check` -> 通过
  - latest guardian 阻断已收敛为两类：`#505` 不能继续冻结 `#504` 所有权下的 request-context 行为；同时必须定义 captured `source_note_id` 如何解析到 canonical `note_id`。本次 head 已据此收口为：current v1 canonical identity = `note_id` only，`image_scenes` not-in-identity，current-detail artifact 存在最小 `source_note_id -> note_id` 解析规则，但不扩写成更广 alias/route truth。

## 回滚

- 回滚方式：如需撤回，执行对应的 revert PR 或回退本 PR 引入的提交。

## 检查清单

- [x] 已确认本 PR 不直接推送主分支
- [x] 已确认标题和提交信息符合中文 Conventional Commits 约束
- [x] 已补充与风险相匹配的验证证据
- [x] 如有对应 Issue，已在 PR 描述中显式写出正确的关闭语义（`Fixes #...` 或 `Refs #...`）
- [x] 若本 PR 属于 formal spec review PR、live evidence 治理落库/治理维护 PR 或落入真实 live evidence 专项门禁，已补齐 `gate_applicability`
- [x] 若本 PR 属于治理相关 lane，已确认 `governance_context_issue_ref`、`review_lane`、精确五文件治理范围与 closing 语义彼此一致，且未混入 FR-0016 formal spec / `docs/dev/specs/FR-0016-live-evidence-governance-gate/TODO.md` 范围
- [x] 若本 PR 落入真实 live evidence 专项门禁，已补齐 latest head 的有效 `live_evidence_record`，且未把 stub/fake host、`runtime.ping` 或 `runtime.bootstrap` 误写为真实闭环证据
- [x] 若事项触及跨仓共享契约、跨仓依赖、联合验收，或当前 PR 改 integration gate / review 语义，PR 描述已补齐 `integration_check`，且提 PR 前与合并前都核对过 `integration_ref`
- [x] 如涉及 FR / 架构 / 高风险目录，已补充必要上下文与影响说明
- [ ] 如涉及正式 spec / 架构规约，已先完成 spec review，且未与实现代码混在同一 PR
- [x] 如本 PR 是正式套件起草 / 修订，已补齐 GWT、异常场景、测试策略与 TDD 范围
- [x] 作者已填写“执行现场自述”，并提供可复核的纯度预检记录

## 变更文件

- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/TODO.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/contracts/detail-canonical-identity.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/data-model.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/plan.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/research.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/risks.md`
- `docs/dev/specs/FR-0026-xhs-detail-canonical-identity/spec.md`

## 自动生成的验证建议

- 已执行：见上文“验证”
- 未执行：guardian rerun、latest-main real-browser rerun
