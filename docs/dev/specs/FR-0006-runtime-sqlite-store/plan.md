# FR-0006 实施计划

## 实施目标

本 FR 的实现只交付 SQLite 最小运行记录基座，形成可查询、可审计、可复用的运行证据层。

本次实现完成后应交付：

- 最小 schema（运行主记录 + 运行事件记录）
- 写入层最小能力（初始化、幂等主记录写入、事件追加写入）
- 查询层最小能力（按 `run_id` 查询主记录与时间线事件）
- 与 FR-0004 对齐的最小诊断字段落库能力
- 与 FR-0003 / FR-0007 对齐的最小引用 / 证据锚点边界

本次不交付：

- 完整业务数据仓库
- 复杂迁移编排系统
- 实时会话状态机真相源

## 分阶段拆分

### 阶段 A：冻结数据模型与边界

产出：

- `data-model.md` 冻结最小实体、字段与约束
- `contracts/runtime-store.md` 冻结写入与查询契约
- 明确与 FR-0003 / FR-0004 / FR-0007 的边界
- 冻结 `run_id` 真相源、诊断 projection、能力证据锚点

依赖：

- `#354/#355` 已完成 formal 收口
- FR-0003、FR-0004、FR-0007 已有 formal 套件可引用

### 阶段 B：实现最小 schema 与初始化

产出：

- SQLite 初始化流程
- schema 版本标识与最小迁移入口（仅当前版本）
- WAL 模式启用与启动校验

依赖：

- 阶段 A 完成

### 阶段 C：实现写入与查询能力

产出：

- 主记录幂等写入
- 事件追加写入
- 按 `run_id` 读取主记录与事件时间线

依赖：

- 阶段 B 完成
- `run_id` 复用 FR-0001 已冻结运行标识
- `profile_name` 复用 FR-0003 已冻结的 profile 边界
- `session_id` 仅作为可空引用字段；若对应 formal 契约后续补齐，可再升级为强依赖

### 阶段 D：接入诊断映射与收口验证

产出：

- 与 FR-0004 对齐的最小诊断字段映射
- 与 FR-0007 对齐的最小能力证据锚点
- 契约测试、边界测试与最小人工验证证据

依赖：

- 阶段 C 完成
- FR-0004 的诊断字段定义冻结
- FR-0007 的能力壳字段定义冻结后，FR-0006 只消费其中需要通过 `run_id` 追溯的最小锚点

最小闭环：

- 到阶段 C 结束，必须已经可以按 `run_id` 查询一次运行的主记录与关键事件。

## 实现约束

- SQLite 不能成为实时会话状态真相源，`runtime.status` 仍由 FR-0003 路径输出。
- SQLite 不能成为能力结果、能力版本或能力健康度的真相源。
- SQLite 不能负责分配 `run_id`，只能消费并持久化上游运行时提供的 `run_id`。
- Phase 1 默认不新增 `capability_*` 主数据列；能力层仅通过 `run_id` 读取最小运行证据。
- 不落敏感原文：Cookie、Authorization、Token、完整 query 参数、完整响应体禁止入库。
- 不改变 FR-0001 的 CLI 外层响应壳和退出码语义。
- 不引入平台业务详情表、全文索引或媒体存储。
- 不在本 FR 内扩张到能力版本仓库或发布系统。

## 测试与验证策略

单元测试：

- schema 初始化与版本检查
- 主记录幂等写入逻辑
- 事件写入关联约束
- 脱敏与截断规则

契约测试：

- `contracts/runtime-store.md` 的输入输出与错误码
- `run_id` 唯一性约束与查询结构稳定性
- 主记录存在前提下事件追加行为
- 诊断 projection 与截断标记兼容性

集成测试：

- 成功路径：命令成功后可查询主记录与至少两条关键事件
- 失败路径：命令失败后仍可查询失败主记录与失败事件
- 锁冲突路径：短时冲突重试与超限错误返回

人工验证：

- 在本地运行一次 CLI 命令，确认可通过 `run_id` 追溯最小时间线
- 验证数据库中无敏感字段明文

完成证据：

- 单元、契约、集成测试通过
- `bash scripts/docs-guard.sh` 通过
- `bash scripts/spec-guard.sh` 通过

## TDD 范围

默认先写测试的模块：

- schema 初始化与版本校验
- 主记录幂等写入
- 事件关联约束
- 脱敏与截断逻辑
- 读模型序列化

暂不强制先写测试的部分：

- CLI 命令端到端人工 smoke
- 极端性能压测

划分理由：

- 前者属于稳定且确定性的共享逻辑，后续 FR-0004 / FR-0007 会复用，必须先测后码。
- 后者依赖运行环境和时序，不适合作为本 FR 的首轮阻断门槛。

## 并行 / 串行关系

前绪阻塞：

- `#354` 已冻结 CLI 契约
- FR-0001 已冻结 `run_id` 运行标识
- FR-0003 已冻结 `profile` 最小身份边界；`session_id` 在本 FR 中只按可空引用字段处理

可并行项：

- 本 FR 的 schema/写入契约设计可与 FR-0004 的诊断字段冻结并行推进
- 本 FR 的运行记录查询契约可与 FR-0007 的能力壳证据锚点并行对齐

串行关系：

- 先冻结 `data-model/contracts`，再进入实现
- 先完成 schema 初始化，再接写入与查询
- 先完成写入正确性验证，再开放后续 FR 消费

## 进入实现前条件

只有满足以下条件，才允许进入 `feat/FR-0006-*` 实现分支：

- 当前 spec-only 变更已在 Draft PR 或等价评审上下文中
- spec review 结论为 `APPROVE`
- `ready_for_implementation = true`
- `data-model.md`、`contracts/runtime-store.md`、`risks.md` 已随套件一起评审通过
- `TODO.md` 中的 review blockers 已清空

在此之前，禁止：

- 修改运行时代码或数据库实现代码
- 将 SQLite 扩张为实时状态真相源
- 将本 FR 与实现代码混在同一 PR
