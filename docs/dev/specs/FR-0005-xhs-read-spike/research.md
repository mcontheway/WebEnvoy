# FR-0005 研究记录（正式输入）

## 研究问题

1. 小红书核心读链路的最小稳定端点集合是什么。
2. 页面签名函数最小调用路径是否可稳定复现。
3. 关键追踪字段在请求级/会话级/刷新级的变化规律是什么。

## 证据要求

1. 每类场景至少两轮独立采样（不同时间窗）。
2. 签名链路至少包含一次成功调用与一次失败样本。
3. 关键字段至少给出三类来源判定证据：页面状态、运行时生成、固定值。

## 当前结论模板

> 本文件在 spec 起草阶段先冻结结构，具体样本结论在侦察执行后补齐。

### 1. 端点结论

- `search`:
- `detail`:
- `user_home`:

### 2. 签名结论

- 入口：
- 输入：
- 输出：
- 已知失效信号：

### 3. 字段生命周期结论

- 请求级变化字段：
- 会话级稳定字段：
- 页面刷新级变化字段：

## 未决项

- [ ] 是否存在灰度版本导致签名入口分流
- [ ] 是否存在设备指纹相关字段仅在特定环境出现
- [ ] 无登录态下是否出现替代端点或降级返回
